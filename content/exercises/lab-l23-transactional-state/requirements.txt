# lab-l23-transactional-state: no third-party Python packages.
# Tested with /usr/bin/python3.12 (CPython 3.12.3) and the standard library only
# (argparse, hashlib, json, os, pwd, queue, re, secrets, shutil, socket, subprocess,
#  tempfile, threading, time, unittest).
# External binaries, not installed by pip: PostgreSQL 16 server tools and client,
# tested with PostgreSQL 16.13 (Ubuntu 16.13-0ubuntu0.24.04.1): postgres, initdb, pg_ctl, psql.
