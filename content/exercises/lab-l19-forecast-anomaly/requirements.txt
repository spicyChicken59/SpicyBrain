# lab-l19-forecast-anomaly: pinned versions actually tested.
# Executed with CPython 3.12.3 in a virtual environment holding these pins;
# scikit-learn brings its own dependencies (scipy, joblib, threadpoolctl), whose
# tested versions are recorded in the lab's evidence file.
pandas==3.0.6
numpy==2.5.3
scikit-learn==1.9.1
