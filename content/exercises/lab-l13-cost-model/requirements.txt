# lab-l13-cost-model: no third-party packages, so there is nothing to pin here.
# The interpreter is the whole environment: tested with CPython 3.12.3 and the
# standard library only (argparse, csv, datetime, decimal, hashlib, importlib,
# json, pathlib, platform, shlex, socket, subprocess, tempfile, unittest).
# Tested only on Python 3.12; no other version was run.
