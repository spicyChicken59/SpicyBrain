# lab-l01-python-parsing: no third-party packages, so there is nothing to pin here.
# The interpreter is the whole environment: tested with /usr/bin/python3.12
# (CPython 3.12.3), standard library only (argparse, csv, datetime, decimal,
# hashlib, importlib, json, logging, pathlib, platform, re, subprocess,
# traceback, unittest). Needs Python 3.11 or later: date.fromisoformat() and the
# policy table depend on 3.11 behaviour, and `float | None` annotations need 3.10.
