# lab-l09-failure-recovery: no third-party packages, so there is nothing to pin here.
# The interpreter is the whole environment: tested with CPython 3.12.3 (python3.12),
# standard library only (argparse, datetime, hashlib, importlib, json, pathlib,
# platform, re, shutil, subprocess, sys, tempfile, time, unittest).
# Only CPython 3.12.3 was run; other 3.10+ interpreters are expected to work but were not tested.
