# Tools the delivery pipeline installs before `databricks bundle deploy` builds the wheel.
# Exact pins, so two runs of one commit build with the same tools. Both versions were
# confirmed to exist on PyPI on 2026-09-23; neither was installed or exercised in this lab,
# which never builds or deploys the wheel. The Databricks CLI is not a pip package: pin its
# version (the lab's guide uses v1.17.0) in whatever step installs it, and print
# `databricks --version` into the pipeline log.
build==1.6.1
setuptools==84.0.0
