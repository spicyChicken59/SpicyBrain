# lab-l16-bundle-delivery: tested with CPython 3.12.3 and exactly this pin.
# Everything else the runner uses is the standard library (argparse, contextlib, copy,
# datetime, decimal, fnmatch, hashlib, importlib, json, pathlib, re, shutil, socket,
# subprocess (only to refuse it), tempfile, tomllib, unittest).
# The Databricks CLI and the Databricks SDK are NOT installed: this lab never contacts a workspace.
pyyaml==6.0.3
