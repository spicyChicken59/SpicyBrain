# lab-l18-point-in-time-features: pinned versions actually tested.
# Executed with CPython 3.12.3 in a virtual environment holding exactly these pins.
pandas==3.0.6
numpy==2.5.3
# pandas' own runtime dependencies as installed in the tested environment
python-dateutil==2.9.0.post0
six==1.17.0
