# Modules 9–16: authoring and editorial review

Authored and audited September 19–20, 2026. The initial primary source passages were reviewed September 19; the additional extension sources below were actually reviewed September 20. A later file write alone is not a new source review. This document records content authoring and deterministic checks. It does not claim customer execution, video playback review by this author, a Databricks lab run, or independent classroom validation.

The eight teaching packages are original explanatory material around 23 preserved canonical lessons. They retain all 69 associated canonical card identities and link each card to the beat that explains its reasoning. Existing lesson prose, question snapshots, diagrams, scenarios and the complete capstone remain available. The course-wide loader continues to report 43 lessons, 144 canonical cards, 96 canonical checks, 18 canonical diagrams, 12 scenarios and one capstone.

## Completed teaching coverage

| Module | Beats / worked visual states | Handbook words | Canonical card links / researched extensions | Editorial decision |
|---|---:|---:|---:|---|
| GenAI, retrieval and agents (`dbxfe-genai`) | 10 / 15 | 1,836 | 6 / 4 | Separate task, selected context, document identity, permission, citation, tool effect, trace diagnosis and evaluation. Ten beats are needed because these boundaries fail independently. |
| Architecture and migration (`dbxfe-m08`) | 10 / 16 | 1,791 | 9 / 4 | Work from responsibilities and constraints to query/copy alternatives, assumptions, aligned populations, offsetting errors, cutover and recovery. A diagram alone cannot establish deployability. |
| Role and teamwork (`dbxfe-m01`) | 8 / 9 | 1,446 | 9 / 4 | Make hypothesis/evidence, decision, ownership, contextual handoff, specialist limits and operational acceptance concrete. Transfer to a fictional energy report. |
| Discovery (`dbxfe-m02`) | 8 / 9 | 1,490 | 9 / 4 | Start from an incident, separate stakeholder meanings and clocks, trace a record, label evidence status and define a measurable denominator. Transfer to dispatch. |
| Demos (`dbxfe-m09`) | 8 / 10 | 1,584 | 9 / 4 | Let audience decisions drive a visible input/rule/output story, correction, replay, explain-back and honest fallback. Adapt the same evidence for a different audience. |
| Proofs of value (`dbxfe-m10`) | 8 / 9 | 1,461 | 9 / 4 | A falsifiable claim leads to a comparable baseline, independent blocking criteria, bounded charter, case coverage, execution record and mixed readout. Transfer to shipments. |
| Competition and business value (`dbxfe-m11`) | 8 / 11 | 1,474 | 9 / 4 | Use workload criteria, documented mechanisms and coexistence before recommendation. Preserve cost boundaries, exact synthetic arithmetic and sensitivity. |
| Field execution and capstone (`dbxfe-m12`) | 8 / 8 | 1,517 | 9 / 4 | Prioritize decision-changing uncertainty; connect follow-up, escalation, discovery, design, demo, PoV, value and ownership. Link the complete canonical exercise and add an inventory transfer. |

Total: 68 beats, 68 authored visuals containing 87 worked states, 12,599 handbook words, 68 new explanation checks and 32 extension cards. Each beat has its own concept definition, visual, question, model answer and reasoning. The explanation checks invite comparison and reflection; they are not automatically scored free-text answers. Two preserved objective checks are also linked directly in the GenAI deck. The other original objective checks remain in their canonical lessons.

Beat explanations range from 46–63 words; handbook entries range from 167–238 words. These counts are an editorial check, not a substitute for instructional quality. Seventeen optional Samajh explanations are 38–47 words each, with explicit mechanism mappings and English boundaries. No analogy is presented as an implementation or proof. Essential definitions appear through explicit concept links rather than incidental hover text.

## What was actually reviewed

For every beat, the content author reviewed the explanation, its visual inputs and resulting decision, the question against its supplied facts, the model reasoning, the handbook example, the transfer boundary, and source context. The final pass corrected local node identifiers and references, made visual text equivalents include actual table values/nodes/relationships, checked every canonical-card mapping, and added missing handbook distinctions about proposed ownership, correction semantics, demo preparation, update versus fix dates, stop conditions, partner availability and reflective learning.

The authored visuals contain inspectable data, conditions or relationships: selected versus wrong manual revisions; allowed versus denied callers; a work-order approval boundary; two traces with different failure locations; aligned versus mismatched reconciliation populations; offsetting row errors; before/after correction totals; pilot acceptance states; and explicit cost equations. Static decision tables remain static when a second state would merely decorate the same explanation. Multi-state visuals change the case or the evidence, not only a highlight.

All eight applied tasks provide a model and reasons. Their final changed-domain cases require replacing domain assumptions while reusing the reasoning. The capstone handbook links to `#/practice/dbxfe-capstone`, retaining the full brief, disclosures, model answer and educational rubric instead of presenting the short beat as the whole capstone.

## Primary-source decisions

Each package contains exact source URLs, publisher, reviewed date, passage summary, cloud/context and caveat. Documented claims reference those source IDs; original field guidance and fictional inputs use separate claim kinds. The original 21 unique cited pages below were reviewed at the publisher. Following the independent extension audit, the eight packages now cite 39 unique publisher URLs; the additional research is recorded below and in each source record. No product benchmark or customer result is inferred from a capability page.

| Source group | Original teaching use and explicit boundary |
|---|---|
| Databricks [AI Search](https://docs.databricks.com/aws/en/ai-search/ai-search), [agent authentication](https://docs.databricks.com/aws/en/agents/custom-agents/agent-authentication), [tracing](https://docs.databricks.com/aws/en/mlflow3/genai/tracing/overview), [evaluation](https://docs.databricks.com/aws/en/mlflow3/genai/eval-monitor/) | AWS product mechanisms. Current AI Search name is distinguished from its former Vector Search name. Relevance is not correctness or permission. App/user identity differ; documented user authorization is Public Preview and the missing-token fallback requires verification in its actual deployment context. Trace and judge output support investigation, not a safety certificate. |
| Databricks [architecture framework](https://docs.databricks.com/aws/en/lakehouse-architecture/) and [external databases/catalogs](https://docs.databricks.com/aws/en/query-federation/) | AWS architecture dimensions and query/catalog federation paths. Query federation and catalog federation have different compute/data paths; neither page is a workload benchmark or source-specific permission approval. |
| AWS [pre-cutover planning](https://docs.aws.amazon.com/prescriptive-guidance/latest/best-practices-migration-cutover/pre-cutover-stage.html), [wave planning](https://docs.aws.amazon.com/prescriptive-guidance/latest/application-portfolio-assessment-guide/wave-planning.html), [communication gates](https://docs.aws.amazon.com/prescriptive-guidance/latest/large-migration-governance-playbook/task-follow-communication-gates.html) | Primary migration examples for ownership, dependency groups, rehearsal, recovery and recording rolled-back results. The original Cinderline runbook does not adopt example durations as commitments. |
| Databricks [go-to-market careers](https://www.databricks.com/company/careers/go-to-market) | Public role context only. No universal L4 title mapping, internal promotion rubric or customer approval authority is inferred. |
| GitLab [SA processes](https://handbook.gitlab.com/handbook/solutions-architects/processes/), [demonstrations](https://handbook.gitlab.com/handbook/solutions-architects/demonstrations/), [PoV](https://handbook.gitlab.com/handbook/solutions-architects/playbooks/pov/), [specialist requests](https://handbook.gitlab.com/handbook/solutions-architects/sa-practices/subject-matter-experts/sme-request/) | Public examples from another employer, supporting contextual preparation and bounded evidence. The PoV page is explicitly a working draft. These are not Databricks policy; internal linked assets were not accessed or reproduced. |
| FinOps Foundation [unit economics](https://www.finops.org/framework/capabilities/unit-economics/) | Cost/unit definitions and technical versus business metrics, with the source's CC BY 4.0 provenance acknowledged. All course figures and examples are original and synthetic, not vendor rates or realized savings. |
| Snowflake [multi-cluster warehouses](https://docs.snowflake.com/en/user-guide/warehouses-multicluster), Google Cloud [external data sources](https://docs.cloud.google.com/bigquery/docs/external-data-sources) | Fair comparison of mechanisms. Snowflake edition/concurrency caveats and BigQuery source/table permission context remain explicit. No overall vendor ranking is derived. |
| Databricks [SQL Server ingestion source setup](https://docs.databricks.com/aws/en/ingestion/lakeflow-connect/sql-server-source-setup), [Free Edition limits](https://docs.databricks.com/aws/en/getting-started/free-edition-limitations), [query profile](https://docs.databricks.com/aws/en/sql/user/queries/query-profile), [billable usage](https://docs.databricks.com/aws/en/admin/system-tables/billing) | Researched extensions add concrete prerequisite, environment and measurement limits. Source access is still customer-specific; Free Edition is not enterprise validation; query execution time is not full pipeline freshness; usage records are not a complete invoice. |

An additional agent-authoring page was reviewed as background without becoming a cited claim: [author agents](https://docs.databricks.com/aws/en/agents/custom-agents/author-agent). The old agent-framework path redirected there. Failed guessed GitLab and Databricks paths were replaced with actual publisher pages. A technical-discovery page containing only a document link was not used to justify detailed methodology from its title. Video selection and playback/transcript evidence belong to the separate media records.

## Numeric and policy checks

All customer people, timings, rows, trace values and financial figures are marked fictional or synthetic. The established unit-based correction example remains 18/1 → 20/1. A provenance conflict may leave a diagnostic candidate of 22/1 while the previous published 20/1 stays stale; even an unkeyed conflicting event blocks publication. Nonconflicting disclosed quarantine can remain publishable. The reliable-data source, tests and archive were not modified by this content author.

Discovery retains the complete population: 18 on-time, one late and one unknown out of 20 gives 90%; removing the unknown gives approximately 94.74% but changes the denominator. Missing timing evidence also has its own completeness consequence.

Value arithmetic preserves 5/10/15 hours per week, 48 weeks, $60 per hour, $12,000 recurring cost and $15,000 implementation cost. Gross modeled value is $14,400/$28,800/$43,200; first-year net is −$12,600/$1,800/$16,200 before omissions; break-even is 9.375 hours per week. At six hours, gross is $17,280 and first-year net −$9,720. Released time is potential capacity until realization is evidenced, not automatic cash savings. Deployment, support, transition and excluded cost assumptions remain visible.

## Verification boundary

The eight modules passed the application teaching schema and the full content loader after final editorial corrections. A deterministic author audit checked handbook/explanation length ranges, nonempty worked visual states, unique canonical-card mappings and malformed nested concept markup. Media registration is recorded separately by the parent task; the content author's complete desktop/narrow visual review is recorded below.

`tests/teaching-state.test.ts` adds 16 executed tests covering predecessor-shaped schema-3 migration, preserved schema-1/2 fixtures, all record families/timestamps, exact teaching position and per-view offsets, check/draft settings in export/import, stale/empty merge, divergent drafts, transactional concurrency, aborted save/recovery/retry, stale preview and replacement failure, future-version refusal, and malformed teaching records. Native IndexedDB version stays 2 while the root becomes schema 4. The old-client case explicitly simulates the baseline schema-3 version guard; it is not described as an old browser bundle execution. All 16 passed with Node 24.19.0. Additional recovery cases exercise the parent's original-collection recovery implementation: malformed/future saved content remains distinct from new unsaved work, including an original payload exceeding 20 MB exported as frames with exact UTF-8 byte counts and no lost content.

`tests/browser/teacher-webkit.spec.ts` passed both real WebKit mobile-emulation journeys against the production build at a nested base path. Touch checks cover a definition that fits the viewport, a changed visual state and enlargement, independent free-text draft/model reveal, handbook beside the deck, Samajh hidden after reload, and absence of fabricated completion or objective scores. The card journey checks the nine core/four extension distinction, exact explanation detour retaining the main saved position, and one explicit extension-card rating creating one review and matching schedule. Both journeys check horizontal overflow. The first run found the definition disappearing after an autosave rerender; the parent fixed component identity, and the full pair then passed. Test-selector and save-poll timing corrections are distinguished from that application defect. This is WebKit device emulation, not a physical iPhone test.


## Independent extension correction, September 20

The independent editorial review found that a source citation did not make several initial extension cards genuinely new teaching: they repeated their module’s core mechanism. Those cards were corrected rather than counted as acceptable because they had citations. `EXTENSION-AUDIT-9-16.json` records all 32 original and final prompts: 22 repeated points were replaced, and ten already adjacent mechanisms were retained with a substantial worked explanation and explicit boundary. All 32 keep their stable IDs and now have revision `2`, a dedicated concept, claim/source links, an answer, a worked explanation and a reason to learn the extension. Existing 144 canonical cards were untouched. The extension explanation is rendered from the same card record in its exact handbook section.

| Module | Replaced repeated core teaching with | Retained and expanded adjacent teaching |
|---|---|---|
| GenAI | Overlapping parallel spans and wall-clock time; protected evaluation holdout after repeated prompt selection | Missing caller-token identity fallback; lexical plus semantic retrieval for exact part numbers |
| Architecture/migration | Partial predicate pushdown; fetch size versus read parallelism | Dependency groups and interface alternatives; latest safe rollback start including verification time |
| Role/teamwork | Superseding architecture decisions; an asynchronous decision trail after a call; system-focused debrief actions | Recording attempted, accepted and rolled-back outcomes separately |
| Discovery | Tail latency hidden by a mean; contextual observation revealing a hidden repair | SQL Server change-tracking prerequisites; released capacity versus realized cash |
| Demos | Free Edition non-commercial scope; color-independent labels; controllable live updates with an honest snapshot time | A cache hit as one possible explanation for a missing query profile |
| PoV | Matched experimental blocks; validating the measurement path; bounded fault injection with a tested stop | Signed usage corrections and delayed complete reporting |
| Competition/value | Snowflake scaling-policy tradeoffs; BigLake access delegation; allocation of shared costs | Cost per successful case with two explicit denominators and acceptance conditions |
| Field/capstone | Incident coordination roles; recurring toil versus an enduring repair; testing corrective-action efficacy; forecast versus approved budget | None of the original four repeated extensions was retained |

New primary sources reviewed at their publishers are OpenTelemetry traces; Google’s ML Crash Course dataset split guidance; Databricks federation performance recommendations; AWS architectural decision records and bounded failure injection; GitLab communication; Google SRE postmortem culture, monitoring, incident management and toil; GOV.UK contextual research; W3C use of color and pause/stop/hide guidance; NIST blocked experiments and measurement-system checks; BigLake introduction; and FinOps cost allocation and forecasting. Exact URLs, reviewed passages, dates, publisher and context are in the package source records. Existing Free Edition and Snowflake multi-cluster pages were rechecked for the new scope and scaling-policy claims. These public examples are not presented as Databricks internal field policy. Accessibility cards teach their particular mechanism without claiming full WCAG compliance. Fault-injection guidance does not authorize production disruption. All new timings, workload quantities and currency figures are explicitly synthetic.

Worked extension arithmetic was reviewed independently of the main examples: overlapping retrieval spans yield a three-second request rather than their five-second duration sum; nine one-minute and one twenty-minute refreshes average 2.9 minutes; a 02:00 boundary minus twenty minutes of rollback and ten of verification leaves 01:30 before any further safety margin; signed usage +10 −10 +12 totals twelve; a shared 3,000-unit bill can be split 1,500/1,500 or 750/2,250 without changing its total; 60/30 and 90/60 accepted cases yield 2 and 1.5 currency units per success. No new example replaces the canonical Cinderline value model.

The same audit corrected the architecture cutover arrow to point from the new active path to the old fallback for rollback, and removed a concept token nested inside the canonical capstone Markdown link. The full content schema/loader passed after all extension changes. The narrow WebKit card test now names the revised Free Edition prompt and asserts its exact extension handbook section; the final integration rerun is recorded separately from the earlier successful pair above.

## Final rendered visual inspection and migration evidence

On September 20, the content author captured all 68 teaching visuals and every one of their 87 states in Chromium 153.0.8010.12 at viewport widths 1440 and 390. The 174 state/viewport captures include actual changed data and conditions, not only first-state previews. Another 63 captures show the right side of wide tables. The author opened and inspected all 41 native-resolution contact sheets containing these 237 images, checking values, relationships, changed states, clipping and legibility. No concrete content or layout defect was found in this pass. A demo total that looked ambiguous in a reduced overview was opened at native size and confirmed as the correct 8/0.

Nodes stack and state controls wrap at the narrow width. Wide tables stay inside labeled, keyboard-reachable scrolling regions with a visible sideways-scroll hint; both left and right positions were inspected. Automated checks confirmed authored titles, node labels, table cells and equations, no page errors and no horizontal page overflow. The narrow rerun also asserted node values and details. This is viewport emulation, not a physical-device or comprehensive assistive-technology certification.

`VISUAL-QA-9-16.json` records every inspected state, source hash, viewport, figure size, table-scroll condition and capture name. All eight source hashes were rechecked after inspection. `visuals-9-16/` contains eight clearly labeled reduced module overviews and four native-size representative images: the GenAI selected-context before/after pair at desktop width, and the migration cutover/rollback pair at narrow width. These twelve PNGs total 6,777,547 bytes. The reduced overviews show initial states for orientation; the complete raw captures and native inspection sheets remain outside the checkout. They are not represented as public full-resolution evidence of every state.

The actual unmodified schema-3 `StudyStore` and content schema from baseline commit `e3bfb97414026e223696312e987879608cc34416` were also executed through `tsx` with `fake-indexeddb`, beyond the earlier literal-version-guard unit test. The genuine schema-3 fixture validated. Opening a schema-4 root left the old client unsaved; attempting a change retained only its new in-memory draft; replacing with an otherwise valid older root was rejected. Persisted root content stayed byte-equivalent after every attempt, and native database version remained 2. `OLD-CLIENT-EXECUTION.json` records this actual old-module execution and the extracted source hashes. It is not an execution of the old browser UI.

`tests/browser/teacher-migration.spec.ts` independently seeds genuine strict schema-3 data into native IndexedDB version 2 before the current app starts. It checks exact migration of every original family and timestamp, creates new teaching state, exports the actual collection, and imports it into a fresh browser context for an exact roundtrip. The parent task reported that the complete 43-journey browser suite passed, including this native migration test and both revised WebKit journeys. The parent records subsequent integration gates separately.
