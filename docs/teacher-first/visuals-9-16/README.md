# Modules 9–16 visual evidence

The eight `*-overview.png` files are labeled **reduced previews**. Each shows every beat's initial visual at desktop and narrow widths for orientation. Their reduced text is not the evidence used for native-size legibility review.

Four unscaled figure captures provide representative before-and-after evidence:

- `1440-dbxfe-genai-beat-context-start.png` and `1440-dbxfe-genai-beat-context-change.png`: selected retrieval context before and after correction, captured at a 1440-pixel viewport (874-pixel figure).
- `390-dbxfe-m08-beat-cutover-start.png` and `390-dbxfe-m08-beat-cutover-change.png`: controlled cutover and an available rollback to the old fallback, captured at a 390-pixel viewport (332-pixel figure).

The twelve PNGs total 6,777,547 bytes. All customer inputs and results are fictional teaching examples. These are real Chromium captures of the production preview, not generated mockups.

The complete inspection is recorded in [VISUAL-QA-9-16.json](../VISUAL-QA-9-16.json): 68 visuals, 87 states, 174 state/viewport captures and 63 additional right-scrolled table captures. The author inspected all 41 native-resolution contact sheets covering those images. Full raw images and native contact sheets remain outside the checkout to keep repository evidence compact. Wide tables intentionally scroll inside their labeled region; the page itself passed overflow checks.

This evidence covers teaching visuals in desktop and narrow viewport emulation. It does not claim a physical-device test, full accessibility certification, or Databricks execution.
