# Approved study-hub milestone

Assigned 19 September 2026 against `spicyChicken59/SpicyBrain` main `a3e858937ef72b0f7a6a474650def0b7b9e8ab23` (tree `72944c6d9bf53140e9e44879325ced2415215f19`). Repository metadata confirmed public, not archived, main default, write permission; the authenticated push dry run succeeded. No open PR existed at preflight. This checkout started clean on `feat/study-hub-reliable-data-foundations`.

The approved outcome is a topic-first study hub plus Reliable Data Foundations: ten complete core topics and two optional bridges, connected original examples, explained practice, local executable Python/Spark exercises, and reliable resumption. This assignment supersedes the original role-first entry point, universal customer explanation, and exactly-seven-section teaching template. It preserves the original 36 lessons and their IDs, assessments, scenarios, diagrams and capstone. It does not claim to reconstruct an unavailable original Markdown brief; the project source available to this builder was its PDF plus the supplied assignment.

Today, Learn, Review and Notebook are the main destinations. Learn presents content-defined roadmaps, direct topic browsing, and playbook references into canonical teaching. Search and Settings remain visible. Existing course/practice/review/notebook/settings and section hash links continue to resolve. Material revisions are visible independently of completion.

One canonical topic can belong to multiple roadmaps. The entered route determines previous/next; a direct topic explicitly falls back to its own course. Optional bridges guide but never lock. Paths, sources, assets, assessments and downloadable exercises are authored content, discovered and validated at build time. A second subject requires no subject-specific application route or registry.

Existing study-data capacity, grandfathering, atomic concurrent imports, immutable events, divergent note/draft preservation, recovery transports, and deterministic review scheduling remain acceptance gates. Schema 3 adds optional path context to exported position records while preserving baseline input compatibility. All browser evidence uses synthetic learner data.

The application remains static React/TypeScript/Vite with local fonts and the shared design snapshot `14a752dd0269bd6ebbb7080eb0d9e1922cd1ef2c` / v2.13.0. README, DESIGN_SYSTEM, VISUAL-RECIPES, CHECKLIST and LICENSE at that immutable reference were inspected read-only. The vendored files were not rewritten. Windows CRLF text may match the exact pinned LF digest after read-only normalization; a changed digest still fails. The loopback test server uses the platform path separator, preserving its containment check on Windows and Linux.

No merge, auto-merge, publication, paid/cloud provisioning, secrets/settings change, schedules, or sibling-repository changes are authorized by this milestone. The existing Site checkpoint is attributed in [DEPLOYMENT.md](DEPLOYMENT.md); this PR does not publish it. Local Python/Spark execution, illustrative Delta instructions, browser results, workflow evidence and builder editorial review are distinct evidence classes. No Databricks workspace execution is claimed.

Acceptance: structural/preservation validation, scoring/review tests, native baseline upgrade and backups, concurrent-state/large-transport regressions, root/nested production builds, real Chromium/WebKit desktop/mobile emulation, narrow/large-text/keyboard/diagram/download checks, content-only photography path/playbook extension and cleanup, source claim review and complete package editorial review. See the milestone acceptance evidence for observed results and limits. Completion means ready for independent review in one PR, not independent acceptance or deployment.
